# Copy this file to .env and fill in real values for local runs.
# python-dotenv is a dependency but must be loaded explicitly if you want
# .env to be picked up automatically — see README "Local setup".
OPENWEATHER_API_KEY=your_openweathermap_api_key_here
